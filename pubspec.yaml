name: esig_utils
description: Utilitarios para serem usados em projetos flutter.
version: 1.0.3
homepage: https://github.com/mobile_esig/esig_utils

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  extended_masked_text: ^1.1.0
  intl: ^0.16.1
  crypto: ^2.1.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  
flutter: